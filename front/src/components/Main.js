import './Main.css'
import { PureComponent } from 'react';

export default class Main extends PureComponent{
    render() {
      return (
        <div className="buttons-container">
            <button className="button all">Показать Все</button>
        </div>
      );
    }
  }
  